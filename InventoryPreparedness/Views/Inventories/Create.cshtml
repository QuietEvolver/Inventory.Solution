@{
  Layout = "_Layout";
}

@model InventoryPreparedness.Models.Inventory

<h4>Add new inventory</h4>

<p><strong>NOTE:</strong> You need to have at least one category before you can add an item!</p>
<p>Go to @Html.ActionLink("this page", "Create", "Categories") to create a category.</p>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Description)
  @Html.TextBoxFor(model => model.Description)

  @Html.LabelFor(model => model.Category)
  @Html.DropDownList("CategoryId")

  <input type="submit" value="Add new inventory" class="btn btn-default" />
}
<p>@Html.ActionLink("Show all categories", "Index")</p>